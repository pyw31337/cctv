name: Z3 Cache Refresh

on:
  # Temporarily manual-only while GitHub Actions minutes are exhausted.
  # Local launchd job `com.pyw31337.cctv.z3-cache-refresh` keeps Oracle and the
  # static GitHub Pages fallback fresh without consuming Actions minutes.
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: cctv-repo-writes
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Refresh and audit Z3 cache
        run: python scripts/refresh_z3_cache.py --max-age-minutes 45 --force --stale-ok-on-refresh-failure-hours 8

      - name: Normalize dashboard timestamps
        if: always()
        run: python3 scripts/standardize_quality_times.py --source github-z3-refresh || true

      - name: Commit and push if changed
        run: |
          if git diff --quiet -- data/z3_cache.json data/cache_status.json data/status.json data/quality_summary.json data/canary_status.json data/ops_status.json data/workflow_status.json; then
            echo "No cache changes"
          else
            scripts/commit_and_push_changes.sh "AUTO: Refresh Z3 appUrl cache [skip ci]" data/z3_cache.json data/cache_status.json data/status.json data/quality_summary.json data/canary_status.json data/ops_status.json data/workflow_status.json
          fi
